# Ensure shell scripts always use LF line endings (required for Linux/Docker)
*.sh text eol=lf

# Go source files — use LF
*.go text eol=lf

# Windows-specific files can use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# JSON, YAML, Markdown — LF
*.json text eol=lf
*.yaml text eol=lf
*.yml  text eol=lf
*.md   text eol=lf

# Dockerfile — LF
Dockerfile text eol=lf
